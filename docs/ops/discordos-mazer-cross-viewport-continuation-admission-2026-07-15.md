# DiscordOS Mazer Cross-Viewport Continuation Admission

Date: `2026-07-15`

## Selection

`mazer-cross-viewport-ui-reliability` is the next active Mazer packet. It is the earliest dependency-safe High-priority primary in the first unfinished critical-path epic after four recovered completions were reconciled. The card remains Open locally and `in_progress` live because this is continuation of an existing non-terminal lane, not a backward lifecycle transition to Ready.

## Exact source truth

- Stable card ID: `mazer-cross-viewport-ui-reliability`.
- Source forum: `1524889569475170478`.
- Source thread/starter: `1525337748830031875`.
- Exact readback: `2026-07-15T07:25:48.295Z`.
- Live lifecycle: `in_progress / 84%`, active and unlocked.
- Canonical boundaries and stable identity: exact.
- Preserved journal count: `9`.
- Readback reason codes: none.

## Bounded product scope

- Reconcile current main and production against the preserved operator physical-device screenshots.
- Finish physical iPhone safe-area and installed-PWA shell-band evidence.
- Finish supported native browser-chrome and resize/maximize/restore evidence.
- Change shared viewport or shell code only when an exact failing surface proves a defect.
- Preserve the completed Options/Pause, defaults, component-standard, graphics, and animation cards.

## Exact live admission receipt

- Event: `mazer-cross-viewport-ui-reliability-continuation-20260715`.
- First apply: source starter updated, continuation journal created as `1526853887925948557`.
- Idempotent replay: source starter updated, the same journal reused, no duplicate message created.
- Identity preflight on both applies: exactly one matching source location, zero collision locations, admitted.
- Starter and journal text/code-point readback: exact on both applies.
- Apply/replay reason codes: none.

## Verification

- Focused board, journal, and consistency suite: `51/51` passed before live mutation.
- Local projection: `64` cards, `0 Ready`, `31 Open`, `14 Completed`; active selector resolves to this Open/in-progress continuation card.
- Exact live event apply and idempotent replay: passed.
- Full DiscordOS `npm run verify`: exit `0` in `350.1s`.
- Final exact source readback at `2026-07-15T07:35:01.132Z`: `in_progress / 84%`, active and unlocked, ten journals, latest journal `1526853887925948557`, stable identity and canonical boundaries exact, no reason codes.
- Final `git diff --check`: passed before commit.

## Non-scope

- No gameplay, scoring, progression, auth-policy, payment, Supabase, or content-system work.
- No legacy/config-wide/full-board Mazer sync.
- No production deployment without a new explicit current-thread Mazer production instruction.
- No mutation of the protected `tests/ai/demo-walker.test.ts`.

## Decisions and questions

None. Existing dependencies, live state, product evidence, and standing operator direction resolve the continuation packet.
